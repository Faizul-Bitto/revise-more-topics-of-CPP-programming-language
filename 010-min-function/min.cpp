#include <bits/stdc++.h>
using namespace std;

int main()
{
    int a, b, c, d, e, f, g, h;
    cin >> a >> b >> c >> d >> e >> f >> g >> h;

    cout << min({a, b, c, d, e, f, g, h}) << endl;

    return 0;
}